const str = "In interview";

let reversedStr = '';
for (let i = str.length - 1; i >= 0; i--) {
  reversedStr += str[i];
}
console.log(reversedStr);

console.log(`-------------------------------------------------------------------`);

const array = [2, 4, 5, 8, 9, 10, 2, 8, 10]
const duplicates = [];

const occurrences = {};

for (let i = 0; i < array.length; i++) {
  const element = array[i];

}

const n1 = "100";
var result = Number("100")
console.log("type of", result);
